# Copy to .env or provide env variables when running k6
# BASE_URL should point to your app (including port), e.g. http://localhost:3000
BASE_URL=http://localhost:3000

# Credentials for an account that can access admin endpoints (optional)
K6_USER=admin@example.com
K6_PASS=changeme

# Debug flag: set to '1' to enable some extra checks/logs in the script
K6_DEBUG=0
