# Example environment variables for production deployment
# Copy this file to the deployment root as `.env` and fill in values.

# Database
# - For local SQLite (docker-compose default), use: NUXT_DB_URL="file:sqlite.db"
# - For remote libSQL/Turso: use the libsql:// URL provided by your provider
NUXT_DB_URL="file:sqlite.db"
# If using a managed provider that requires a token
NUXT_DB_AUTH_TOKEN=""

# App runtime
NODE_ENV=production

# Cloudflare Tunnel: either provide a CLOUDFLARED_TUNNEL_TOKEN for token mode,
# or configure cloudflared credentials and mount them into the container.
CLOUDFLARED_TUNNEL_TOKEN=""

# BetterAuth / any 3rd-party secrets (if used)
# e.g. sendgrid API keys, OAuth client secrets, etc.
# AUTH_SENDGRID_API_KEY=""
# OAUTH_CLIENT_ID=""
# OAUTH_CLIENT_SECRET=""

# Optional: allow overriding Nuxt host/port
NUXT_HOST=0.0.0.0
NUXT_PORT=3000

# Optional: other values your deployment uses
# For example, if you use a libsql provider set the provider-specific envs
# LIBSQL_URL="libsql://..."
# LIBSQL_TOKEN="..."
